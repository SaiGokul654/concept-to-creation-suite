import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.concepttocreationsuite',
  appName: 'ConceptToCreationSuite',
  webDir: 'dist'
};

export default config;
